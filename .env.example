# Optional: override contact form POST URL (default /api/contact)
# VITE_CONTACT_API_URL=https://your-domain.vercel.app/api/contact

# SMTP variables belong in Vercel/host dashboard (server-side), not in VITE_ — see README.
